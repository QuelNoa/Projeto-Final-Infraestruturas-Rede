apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: resilience
spec:
  selector:
    app: dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP